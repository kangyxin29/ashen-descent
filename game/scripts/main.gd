extends Node2D
## Main — scene tree is built in main.tscn.
## This script can hold global boot logic for later milestones.
## For the prototype shell, the scene is self-contained.

func _ready() -> void:
	print("Ashen Descent — prototype room loaded")
	print("WASD / Arrow keys to move")
